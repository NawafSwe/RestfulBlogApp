<%- include ('partials/header') %>

<% blogs.forEach((blog)=>{%>
    <h2><%= blog.title%></h2>
    <img src="<%=blog.image_url%>">
    <p><%=blog.created%></p>
<%})%>
<%- include ('partials/footer') %>